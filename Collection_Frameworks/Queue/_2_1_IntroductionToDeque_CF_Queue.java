/**--> Deque is Double ended queue.
 * --> Java Deque Interface is a linear collection that supports element insertion and removal at both ends.
 * --> public interface Deque<E> extends Queue<E>
 * --> LinkedList and ArrayDeque implements Deque interface.
 * */

package Collection_Frameworks.Queue;

import java.util.ArrayDeque;
import java.util.Deque;
import java.util.LinkedList;

public class _2_1_IntroductionToDeque_CF_Queue {
    public static void main(String[] args) {
        //Representation
        Deque lLL=new LinkedList();
        Deque aADQ=new ArrayDeque();
    }
}
